# Figma REST API import (link / file key). Create at https://www.figma.com/developers/api#access-tokens
FIGMA_ACCESS_TOKEN=

# If verify/import shows "fetch failed" on corporate VPN, set to true for local dev only:
# FIGMA_SKIP_TLS_VERIFY=true
