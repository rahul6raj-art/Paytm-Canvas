"use client";

/**
 * Auto canvas→source sync is disabled. Source files update only when the user
 * chooses Update source code (context menu) or Export to source.
 */
export function CraftBridgeAutoSync() {
  return null;
}
