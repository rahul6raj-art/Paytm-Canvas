/** Recursive measure pass — delegates to `@/lib/layoutEngine/measure`. */
export {
  measureNode as measureAutoLayoutNode,
  resolveHugSize,
  resolveContentSize,
  type MeasureContext,
  type MeasuredChild,
} from "@/lib/layoutEngine/measure";
